/**
 * Normalized anchor points (% of preview frame) and default scales per preset.
 * Source of truth: `lib/placement-profiles.ts` (PROFILE_CATALOG).
 */
export { ALL_PRESET_BY_ID, ALL_PRESETS, presetById } from './placement-profiles';
